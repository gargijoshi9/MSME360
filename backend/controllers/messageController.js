/**
 * NOT USED — Phase 1 (auth module)
 *
 * messageController.js will be implemented in Phase 2.
 * It will handle:
 *   - GET /api/messages          — list messages (filtered + paginated)
 *   - GET /api/messages/search   — keyword search via MongoDB $text index
 *
 * Note: search is MongoDB $text only (no embeddings, no vector search).
 * See models/DecisionEvent.js for the text index definition.
 */
