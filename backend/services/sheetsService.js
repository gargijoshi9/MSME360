/**
 * NOT USED — Phase 1 (auth module)
 *
 * sheetsService.js will be implemented in Phase 2.
 * It will provide write-only access to Google Sheets using the user's
 * OAuth2 tokens (stored in User.googleAccessToken / User.googleRefreshToken).
 *
 * Dependencies needed (Phase 2): googleapis
 */
