/**
 * NOT USED — Phase 1 (auth module)
 *
 * gmailService.js will be implemented in Phase 2.
 * It will provide:
 *   - OAuth2 client with auto token refresh (persisting to User.googleTokens)
 *   - Pub/Sub watch setup (must be renewed every 7 days)
 *   - Message fetching by ID
 *
 * Dependencies needed (Phase 2): googleapis
 */
