/**
 * NOT USED — Phase 1 (auth module)
 *
 * searchService.js will be implemented in Phase 2.
 * It will query the MongoDB $text index on DecisionEvent.content.text
 * and DecisionEvent.content.summary.
 *
 * No embedding / vector search — the index is plain MongoDB full-text only.
 * See models/DecisionEvent.js for the index definition.
 */
