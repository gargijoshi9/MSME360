/**
 * NOT USED — Phase 1 (auth module)
 *
 * classifierService.js will be implemented in Phase 2.
 * It will call an LLM (e.g. Gemini Flash) to classify inbound messages,
 * returning { category, priority, summary }.
 *
 * NO FAKE FEATURES: Do not add a placeholder that pretends to be an LLM
 * (e.g. a hash-based fake classifier). A missing feature is honest;
 * a fake one is a liability.
 */
